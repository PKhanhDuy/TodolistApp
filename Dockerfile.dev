# syntax=docker/dockerfile:1

# Development image with hot reload (Vite dev server)
FROM node:22-alpine
WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

EXPOSE 5173
CMD ["npm", "run", "dev", "--", "--host"]
